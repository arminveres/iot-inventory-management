use aries_vcx_agent::{self, PoolInitConfig, AgencyInitConfig, WalletInitConfig};

fn main() {
    // let config = aries_vcx_agent::InitConfig {
    //     enterprise_seed = 0,pool_config=PoolInitConfig,agency_config=AgencyInitConfig,wallet_config=WalletInitConfig
    // };
    // let test = aries_vcx_agent::Agent::initialize();
    println!("Hello, world!");
}
